import styled from 'styled-components';
import { SPACES } from '../../../../../theme';

export const Wrapper = styled.div`
  display: flex;
  width: 100%;
  align-items: center;
  gap: ${SPACES.m};
`;
